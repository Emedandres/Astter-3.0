import 'package:flutter/material.dart';

class clasesStudent extends StatelessWidget {
  const clasesStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text('Clases'),
    ));
  }
}

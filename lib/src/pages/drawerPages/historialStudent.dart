import 'package:flutter/material.dart';

class HistorialStudent extends StatelessWidget {
  const HistorialStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Historial'),
      ),
    );
  }
}
